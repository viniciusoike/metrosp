% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_metro_demand.R
\name{read_metro_demand}
\alias{read_metro_demand}
\title{Read Metro SP demand data}
\usage{
read_metro_demand(
  dataset = c("passengers_entrance", "passengers_transported", "station_averages",
    "station_daily"),
  source = c("auto", "cache", "remote", "bundled"),
  vintage = "latest",
  cache = TRUE,
  quiet = FALSE
)
}
\arguments{
\item{dataset}{Dataset to read. One of \code{"passengers_entrance"},
\code{"passengers_transported"}, \code{"station_averages"}, or \code{"station_daily"}.}

\item{source}{Where to read from.
\itemize{
\item \code{"auto"} (default) uses the cache, downloads when it is stale or empty,
and falls back to the bundled snapshot with a warning if the download
fails.
\item \code{"cache"} reads only what is already on disk and errors otherwise.
\item \code{"remote"} downloads and errors if that fails.
\item \code{"bundled"} reads the frozen snapshot and never touches the network.
}}

\item{vintage}{Which published batch to read. \code{"latest"} tracks the rolling
release; a year-month string such as \code{"2026-08"} pins an immutable batch.}

\item{cache}{Whether to write downloads to \code{\link[=metrosp_cache_dir]{metrosp_cache_dir()}}.}

\item{quiet}{Whether to suppress progress messages.}
}
\value{
A data frame. See \link{passengers_entrance}, \link{passengers_transported},
\link{station_averages}, and \link{station_daily} for the column definitions, which
are identical across sources.
}
\description{
Reads one of the four passenger demand datasets, preferring the most
recently published version over the frozen snapshot bundled with the
package. Published data lives in the repository's GitHub releases and is
rebuilt from the upstream sources on every pipeline run.
}
\details{
Only the demand datasets are published separately. The reference datasets
(\link{lines}, \link{stations}, \link{station_inauguration}, \link{calendar_spo}, and
\link{metro_colors}) do not change with new months, so read them directly.

Downloads verify the manifest's SHA-256 when the \pkg{digest} package is
installed and skip verification otherwise.
}
\examples{
# The bundled snapshot needs no network access.
entrance <- read_metro_demand("passengers_entrance", source = "bundled")
head(entrance)

\dontrun{
# The most recently published build.
daily <- read_metro_demand("station_daily")

# A pinned vintage, for a reproducible analysis.
daily_aug <- read_metro_demand("station_daily", vintage = "2026-08")
}
}
\seealso{
\code{\link[=metrosp_cache_dir]{metrosp_cache_dir()}} and \code{\link[=metrosp_cache_clear]{metrosp_cache_clear()}} for cache
management.
}
